# Slides

This directory contains a 20-slide Typst presentation using the diatypst package.

Compile from the project root with:

```bash
typst compile --root . report/slides/main.typ report/slides/main.pdf
```

The deck intentionally reserves six slides for final results analysis and one
slide for conclusions.
